---
fullName: Pierre Symon Peralta
position: Front End Developer
images:
  - parrot.gif
  - ….jpeg
  - "!.gif"
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ fullName }}</title>
</head>
<style>

  .image-container {
    display: flex;
  }
  .image-card {
    display: flex;
    flex-direction: column;
    border: 0.2px solid #000;
    padding: 4px;
    margin: 4px;
  }

  .image-card img {
    width: 180px;
  }
  
</style>
<body>
  <main>
    <div class="site-banner">
      <div>
        <h1>{{ fullName }}</h1>
        <h3>{{ position }}</h3>
      </div>
      <div class="image-container">
        {%- for filename in images %}
          {%- capture path %}img/{{ filename }}{% endcapture %}
          <div class="image-card">
            <img src="img/{{ filename | url_encode }}" alt="{{filename}}">{{ path | custom-filter-filesize }}
          </div>
          {%- endfor %}
      </div>
    </div>
    <div class="site-body">
      {{ content | safe }}
    </div>
  </main>
</body>
</html>